package lesson6.calculator;

abstract class EasyCalculator {
    abstract int addition (int firstNumber, int secondNumber);
    abstract int subtraction (int firstNumber, int secondNumber);
    abstract long multiplication (int firstNumber, int secondNumber);
    abstract int division (int firstNumber, int secondNumber);
}
